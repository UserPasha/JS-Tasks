function sequence(x) {
    let a=[]
    let i=0
    while (x>0){
      x=x-1;
      i++;
      a.push(i)
      }
     return a.sort()
   }

   const num = 8
   console.log(sequence(8))


   // timmy
   // dogs
   // big
   // bigger

   // &lt;      (<)